---
name: devops-build
description: 量潮 DevOps 构建管理。当用户说"构建"、"build"、"编译"时使用。
---

# devops-build — 构建管理

## 命令

| 命令 | 用途 |
|------|------|
| `qtcloud-devops build status` | 查看构建状态（编译、依赖、版本一致性） |

`build` 只有读操作，无写入。
